import sys, pygame, random
from Mediator import *

class Generator:

    def __init__ (self, screen, mediator):
        self.screen = screen
        self.mediator = mediator
        self.level = level

    def generate (self):
        if level == 0:
            ##existing code
        elif level == 1:
            

